# Formspree CLI deploy key.
# Find it in your Formspree project → Settings → "Deploy key".
# Copy this file to .env and paste the real key there. .env is gitignored,
# so the secret never ends up in the repository.
FORMSPREE_DEPLOY_KEY=your-deploy-key-here
