.claude/
.DS_Store

# Build tooling
build/node_modules/

# Secrets — never commit the Formspree deploy key (or any env file)
.env
.env.*
!.env.example

# Interne werkdocumenten — alles in de repo-map wordt door GitHub Pages
# meegepubliceerd, dus briefings en concepten mogen er nooit in.
pagina-brief.md
*-brief.md
werkdocumenten/
